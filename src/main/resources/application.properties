# Server Configuration
server.port=8080

# AES Encryption Key (128-bit = 16 bytes encoded in Base64)
aes.encryption.key=ABEiM0RVZneImaq7zN3u/w==

# Fields to encrypt (comma-separated)
encryption.fields=ssn,email,creditCard,phone,password

# CORS configuration
cors.allowed.origins=http://localhost:3000